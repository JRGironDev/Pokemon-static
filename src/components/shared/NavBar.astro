---
const links = [
  { href: "/", label: "Listado" },
  { href: "/pokemons/1", label: "Paginado" },
  { href: "/favorites", label: "Favoritos" },
];
---

<nav class="shadow bg-slate-800">
  <div
    class="container flex items-center justify-center p-6 mx-auto text-gray-600 capitalize dark:text-gray-300"
  >
    {
      links.map(({ href, label }) => (
        <a
          href={href}
          class="text-gray-800 dark:text-gray-200 border-b-2 border-transparent hover:text-gray-800 dark:hover:text-gray-200 hover:border-blue-500 mx-1.5 sm:mx-6"
        >
          {label}
        </a>
      ))
    }
  </div>
</nav>
